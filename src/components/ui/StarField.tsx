
"use client";

// TODO: Install @react-three/fiber and @react-three/drei packages to enable StarField
// import { Canvas } from "@react-three/fiber";
// import { Stars } from "@react-three/drei";

const StarField = () => {
  // Placeholder until three.js dependencies are installed
  return (
    <div className="fixed inset-0 -z-10 bg-gradient-to-b from-background to-muted/20" />
  );
  
  // return (
  //   <Canvas>
  //     <Stars radius={50} count={2500} factor={4} fade speed={2} />
  //   </Canvas>
  // );
};

export default StarField;
