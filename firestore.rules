
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access for development purposes.
    // WARNING: This is not secure for production.
    // We will implement proper authentication and rules later.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
