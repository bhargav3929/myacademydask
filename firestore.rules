
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(orgId) {
      return request.auth.token.role == 'owner' && request.auth.token.organizationId == orgId;
    }
    
    function isCoach(orgId) {
       return request.auth.token.role == 'coach' && request.auth.token.organizationId == orgId;
    }
    
    function getRole() {
      return request.auth.token.role;
    }
    
    function getOrgId() {
        return request.auth.token.organizationId;
    }

    // USERS:
    match /users/{userId} {
       // Allow read if user is reading their own document, is an owner of the org, or is admin
       allow read: if isSignedIn() && 
                    (request.auth.uid == userId || isOwner(resource.data.organizationId));
      // Allow update if user is updating their own document or is admin
      allow update: if isSignedIn() && request.auth.uid == userId;
      // Allow create for signed in users (needed for account creation)
      allow create: if isSignedIn(); 
      allow delete: if false;
    }

    // ORGANIZATIONS:
    match /organizations/{orgId} {
        allow read, update: if isSignedIn() && isOwner(orgId);
        allow create, delete: if false;
    }

    // STADIUMS:
    match /stadiums/{stadiumId} {
        allow read: if isSignedIn() && (isOwner(getOrgId()) || isCoach(getOrgId()));
        allow create, update: if isSignedIn() && isOwner(getOrgId());
        allow delete: if false;

        // STUDENTS Subcollection (Directly under stadium - for owners)
        match /students/{studentId} {
            allow read, create, update, delete: if isSignedIn() && isOwner(getOrgId());
        }

        // COACHES Subcollection in a Stadium
        match /coaches/{coachId} {
             allow read, write: if false; // Only allow access to nested subcollections

            // STUDENTS under a specific COACH
            match /students/{studentId} {
                allow read, create, update, delete: if isSignedIn() &&
                                                        (isOwner(getOrgId()) || (isCoach(getOrgId()) && request.auth.uid == coachId));
            }
        }

        // ATTENDANCE Subcollection
        match /attendance/{attendanceId} {
             allow read, create, update, delete: if isSignedIn() &&
                                                     (isOwner(getOrgId()) || isCoach(getOrgId()));
        }
    }
    
    // ATTENDANCE SUBMISSIONS
    match /attendance_submissions/{submissionId} {
        allow create: if isSignedIn();
        allow read: if isSignedIn() && isOwner(getOrgId());
        allow update, delete: if false;
    }

    // SUPER ADMIN
    match /stadium_owners/{ownerId} {
        allow read, create, update, delete: if isSignedIn();
    }
  }
}
